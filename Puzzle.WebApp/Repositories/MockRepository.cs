namespace Puzzle.WebApp.Repositories
{
    //public class MockRepository : Mock<IRepository<Todo>>
    //{
    //    public MockRepository()
    //    {
    //        Setup(x => x.GetById(It.IsAny<int>()))
    //            .ReturnsAsync((int id) =>new Todo() { Id = id});

    //        Setup(x => x.Add(It.IsAny<Todo>()))
    //            .ReturnsAsync((Todo entity) => entity);

    //        Setup(x => x.Update(It.IsAny<Todo>()))
    //            .ReturnsAsync((Todo entity) => entity);

    //        Setup(x => x.Delete(It.IsAny<Todo>()))
    //            .Returns(Task.CompletedTask);
    //    }
    //}
}